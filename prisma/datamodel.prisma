type User {
  id: ID! @id
  email: String! @unique
  name: String
  password: String
}

type Session {
  id: ID! @id
  user: User!
  createdAt: DateTime! @createdAt
}

type Permissions {
  id: ID! @id
  title: String!
  description: String
}

